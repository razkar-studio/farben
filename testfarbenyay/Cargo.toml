[package]
name = "testfarbenyay"
version = "0.1.0"
edition = "2024"

[build-dependencies]
farben-build = { path = "../farben-build", version = "0.1.0" }

[dependencies]
farben = { version = "0.16.2", path = "../farben", features = ["compile"] }
farben-core.workspace = true
